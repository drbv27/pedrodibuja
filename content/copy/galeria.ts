// Copy module for "/galeria" (deck section 4.3). Establishes the per-page
// copy module structure alongside `content/site.ts`; the deck-verbatim text
// ships with this route's page in a later work unit.
export {};
