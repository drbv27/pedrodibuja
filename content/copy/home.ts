// Copy module for "/" (deck section 4.1). Establishes the per-page copy
// module structure alongside `content/site.ts`; the deck-verbatim text and
// null-safe sentence variants for this route ship with the home page itself
// in a later work unit.
export {};
