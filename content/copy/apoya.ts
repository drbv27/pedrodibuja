// Copy module for "/apoya" (deck section 4.5). Establishes the per-page copy
// module structure alongside `content/site.ts`; the deck-verbatim text and
// the `flags.commerce`-gated blocks ship with this route's page in a later
// work unit.
export {};
