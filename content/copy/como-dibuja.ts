// Copy module for "/como-dibuja" (deck section 4.4). Establishes the
// per-page copy module structure alongside `content/site.ts`; the
// deck-verbatim text ships with this route's page in a later work unit.
export {};
