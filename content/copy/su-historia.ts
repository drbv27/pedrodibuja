// Copy module for "/su-historia" (deck section 4.2). Establishes the
// per-page copy module structure alongside `content/site.ts`; the
// deck-verbatim text and the conditional Bloque 2 / Bloque 4 narrative
// handling ship with this route's page in a later work unit.
export {};
